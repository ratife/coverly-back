services:
  database:
    image: postgres:13
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: ads
    ports:
      - "5432:5432"
    volumes:
      - data_postgres:/var/lib/postgresql/data

volumes:
  data_postgres: